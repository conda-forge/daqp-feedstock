schema_version: 1

context:
  version: "0.9.1"

package:
  name: daqp
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/d/daqp/daqp-${{ version }}.tar.gz
  sha256: 5c25df894114bda14ad6db5179a3210fc6a3510e71b6196ef488dd809a139443

build:
  number: 0
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  build:
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - cython
    - ${{ compiler('c') }}
    - ${{ stdlib('c') }}
  host:
    - python
    - cython
    - setuptools
    - pip
  run:
    - python
    - numpy

tests:
  - python:
      imports:
        - daqp
  - requirements:
      run:
        - pip
        - python
    script:
      - pip check

about:
  summary: "DAQP: A dual active-set QP solver"
  license: MIT
  license_file: LICENSE
  homepage: http://github.com/darnstrom/daqp

extra:
  recipe-maintainers:
    - darnstrom
    - flferretti
